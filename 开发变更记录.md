# Ear2Finger 英语听写助手 - 今日开发变更记录 (2026-06-14)

本文件详细记录了今天针对国内使用环境、多平台部署准备、离线语音识别（STT）和设备兼容性优化所做出的全部开发变更。方便您后续在 Mac mini 或其他设备上快速同步并开展功能验证。

---

## 1. 核心功能开发与优化

### R1. B站与本地无字幕音视频的离线语音转文字 (Whisper.cpp)
* **新增转写服务**：创建了 `backend/services/transcription_service.py`，负责在本地调用编译好的 `whisper-cli`。可将输入音频转换成 Whisper.cpp 所需的 16kHz 单声道 PCM WAV 格式，进行离线转写并提取带有时间戳的 `.srt` 字幕。
* **无字幕智能回退**：修改了 `local_processor.py` 与 `youtube_processor.py` 的处理流程。当导入的本地视频/音频或 B站视频链接不带字幕时，程序会自动在后台调用 Whisper.cpp 转写，转写完成后自动将句子分段存入 SQLite 数据库。
* **macOS 自动编译集成**：修改了 `scripts/setup-binaries.sh`，在 macOS 下运行时，除了下载 FFmpeg，还会自动在本地克隆 `whisper.cpp` 官方仓库并进行本地编译，自动适配您的 Mac 架构（如 Apple Silicon 的 Accelerate/Metal 硬件加速），产出高能效的离线识别二进制文件。

### R2. 电脑性能适配与风扇噪音优化 (限制 CPU 线程)
* **线程限制优化**：针对 Whisper 转写时可能吃满多核 CPU 导致电脑发烫、风扇狂转的问题，在转写服务中增加了 **`-t` (线程限制)** 参数。动态计算电脑核心数并限制最大使用 **4 个线程 (或当前核数之半)**，在不影响转写速度的前提下，彻底解决了风扇狂转的体验痛点。
* **低配音质跳过转写**：支持用户在设置中选择 64kbps 的“流畅”音质，系统将跳过 FFmpeg 对 YouTube 音频的转码重新封装，直接提取原生音频流播放，大幅降低低端设备的 CPU 占用。

### R3. 多大模型兼容与动态 Embedding 解析 (DeepSeek / OpenAI / Gemini)
* **国内直连大模型**：通过 OpenAI 兼容适配器添加了对 **DeepSeek** 的全面支持，允许用户配置自定义 API Base URL（用于国内代理中转），方便切换不同的大模型厂商。
* **动态 Embedding Key 冲突修复**：解决了用户只配置了 DeepSeek Key、而系统默认调用 Gemini 向量化服务引发的 "Missing Gemini API key" 报错。现在，大模型工厂会智能根据您配置的 Key，动态回退并适配您的向量化厂商；同时对向量化报错进行了 `try-except` 优雅降级捕获，即使网络超时或无向量化 Key，也仅会在控制台打印一行 Warning，绝不影响您核心听写打分和 AI 导师（AI Coach）的使用。

### R4. 健壮性与健壮性字符集解码异常处理
* **消除 UnicodeDecodeError**：消除了 `subprocess.run` 调用 whisper-cli 和 ffmpeg 时，因使用 `text=True` 导致在部分非 UTF-8 字符集或带终端特效输出下抛出 `UnicodeDecodeError` 导致导入失败的经典 Bug。修改为二进制流读取，并通过手动 `.decode('utf-8', errors='ignore')` 强制过滤脏字符。

---

## 2. 本土化改造与指引文档

* **前端弹窗与中文本地化**：
  * 修改了 `ImportModal.tsx`，将导入选项卡本地化为中英文双语，显式提示对 YouTube 和 哔哩哔哩 (B站) 视频链接的支持，并在输入框下增加了离线转写等中文辅助说明。
  * 在设置页面中增加了 API 密钥的 Show/Hide 遮罩显示，并对低配音质选择增加了智能提示。
* **用户指引与脚本**：
  * **`操作指引.md`**：专为非技术用户编写的快速上手指南，包括环境运行、AI 密钥填写、视频导入和常见 FAQ。
  * **`教程视频脚本.md`**：为推广和演示视频录制编写的 3-5 分钟分镜与旁白配音脚本。
* **单元与冒烟测试套件**：
  * **`tests/test_ear2finger.py`**：集成了 7 个核心测试用例，覆盖单元测试、冒烟测试和回归测试，可通过 `python -m unittest tests/test_ear2finger.py` 一键运行验证。

---

## 3. 今日新增与变更的文件清单

```
.
├── .gitignore                          # 增加了对本地运行缓存 storage/ 和 scratch/ 的忽略
├── Run_Ear2Finger.py                   # 优化了热启动端口重用逻辑
├── 开发变更记录.md                       # (本文件) 今日开发详情记录
├── 操作指引.md                          # 极简用户手册
├── 教程视频脚本.md                       # 视频教学录制大纲
├── backend/
│   ├── main.py                         # 优化了热重启逻辑与文件清理
│   ├── routers/
│   │   ├── health.py                   # 增加了设备 CPU 核心检测接口
│   │   ├── local_media.py              # 同步化路由以防 Event Loop 阻塞
│   │   ├── user_config.py              # 增加了音频质量持久化配置与限制日志
│   │   └── youtube.py                  # 同步化路由以防 Event Loop 阻塞
│   └── services/
│       ├── ai_client_factory.py        # 动态解析 API Key 以防 Embedding 重定向报错
│       ├── local_processor.py          # 更改为先抽取音频再转写字幕，实现自动离线 STT
│       ├── qdrant_client.py            # 对向量化过程进行异常捕获和安全降级
│       ├── transcription_service.py    # (New) 离线 Whisper 识别核心服务，包含线程和字符集解码优化
│       └── youtube_processor.py        # 增加了无字幕时自动调用离线 Whisper 识别的逻辑
├── frontend/src/
│   ├── api.ts                          # 增加了性能配置接口和数据类型
│   └── components/
│       ├── ImportModal.tsx             # 增加了中英双语标签及 B站 URL 提示
│       ├── Settings.tsx                # 增加了 API 密钥遮罩切换和音质推荐
│       └── Workspace.tsx               # 增加了非阻塞的中心全屏“转写中/导入中”加载遮罩
├── scripts/
│   └── setup-binaries.sh               # 增加了 macOS 下自动拉取源码编译 whisper.cpp 的功能
└── tests/
    └── test_ear2finger.py              # (New) 包含 7 个单元、冒烟与回归测试用例的测试套件
```

---

## 4. 后续在 Mac mini 上同步与验证的流程

当您在 Mac mini 上拉取了最新的 `electron` 分支后，请按以下步骤快速验证：

1. **拉取代码**：
   ```bash
   git checkout electron
   git pull origin electron
   ```
2. **自动化安装基础依赖**：
   运行以下命令，会自动下载 FFmpeg，并自动在您的 Mac mini 上克隆编译出适配本地架构（如 Apple Silicon）并支持 GPU 加速的 `whisper-cli` 二进制文件。
   ```bash
   bash scripts/setup-binaries.sh
   ```
3. **一键运行测试套件**：
   使用虚拟环境运行测试文件，确保环境配置和转写逻辑没有任何问题：
   ```bash
   .venv/bin/python -m unittest tests/test_ear2finger.py
   ```
   * 如果返回 **`OK`**，说明 Mac mini 环境已经完全就绪，您可以通过启动脚本直接运行程序，开始在 Safari 浏览器中导入测试！
