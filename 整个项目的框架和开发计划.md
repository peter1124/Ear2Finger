# 整个项目的框架和开发计划

## 1. 项目框架 (Architecture Framework)

### 1.1 开发与编译环境分离
- **开发中心 (Mac mini M4):** 负责代码重构、前端构建、后端逻辑验证。
- **编译中心 (MBP 2017 - Windows 10):** 负责最终的 x64 架构二进制打包。

### 1.2 核心逻辑收拢 (Consolidation)
- **后端托管前端:** 移除对独立 Node.js 前端服务的依赖，由 FastAPI 直接托管 `frontend/dist`。
- **Browser-as-UI 策略:** 彻底抛弃 Electron 壳子。用户双击 `.exe` 后，后台静默启动 FastAPI，并自动调用 Windows 系统默认浏览器（Edge/Chrome）作为 UI 界面。
- **生命周期同步 (自杀锁):** 实现一种网络连接监测机制，当浏览器标签页关闭（TCP 连接归零）时，后端进程自动检测并安全退出，确保不占用系统后台资源。
- **环境自适应:** 后端需能识别当前是开发环境还是 PyInstaller 打包后的二进制环境，并据此调整静态资源和二进制依赖的路径。
- **跨平台依赖替换:** 
    - **开发期 (Mac mini):** 使用 `brew install ffmpeg` 提供的 ARM64 版本进行功能验证。
    - **编译期 (MBP Win10):** 在打包前将 `ffmpeg.exe` (x64 Windows 版) 替换入 `bin/` 目录。

## 2. 开发计划 (Development Roadmap)

### 第一阶段：Mac mini 上的架构重构 (正在进行)
1.  **前端静态化:** 
    - 执行 `npm run build` 生成生产环境产物。
    - 验证静态资源在 FastAPI 中的挂载逻辑。
2.  **路径解耦 (Path Decoupling):**
    - 修改 `backend/main.py` 和 `backend/config.py`，实现基于 `sys.executable` 的路径锁定。
    - 动态注入 `bin/` 目录到 `PATH` 环境变量。
3.  **定制功能开发 (不重复造轮子):**
    - **B站解析优化:** 针对 B站上的英文视频，优化 `yt-dlp` 参数以获取双语或中英字幕，确保听写练习的语料丰富。
    - **本地导入模块:** 开发新的 API 接口，接收本地文件流或路径，调用 `ffmpeg` 进行预处理（提取音频、生成波形）。
    - **DeepSeek 集成:** 在 `ai_client_factory.py` 中引入 `ChatOpenAI` 或 `ChatDeepSeek`，允许通过 `.env` 灵活切换 `openai_api_base`。
4.  **编写守护程序 (Launcher):**
    - 实现 `Run_Ear2Finger.py` 脚本，集成 `subprocess` 静默启动和 `netstat` 网络锁逻辑。
5.  **全链路验证:** 
    - 在 Mac mini 上仅运行 `python main.py`，验证浏览器是否能完整操作所有功能。

### 第二阶段：跨设备代码同步
1.  **本地 Git 同步:** 
    - 在 Mac mini 上 Commit 代码。
    - 在 MBP 2017 (Win10) 侧通过局域网 Git Clone 同步源码。

### 第三阶段：Windows 侧打包与交付
1.  **环境配置:** 在 Windows 上安装 `uv`，创建虚拟环境并安装 `requirements.txt`。
2.  **二进制收集:** 将 Windows 版 `ffmpeg.exe` 放入 `bin/` 目录。
3.  **执行打包:** 
    - 使用 PyInstaller 编译后端 `backend.exe`。
    - 使用 PyInstaller 编译启动器 `启动听写.exe`。
4.  **交付验证:** 在 Windows 上运行绿色压缩包，进行最终功能测试。

## 3. 核心重构任务清单 (Refactoring Checklist)

- [x] **Frontend Build**: `frontend/dist` 产出。
- [x] **FastAPI Static Mounting**: `app.mount("/", StaticFiles(directory=FRONTEND_DIST, html=True))`。
- [x] **Binary Path Locking**: `os.path.join(BASE_DIR, "bin", "ffmpeg.exe")` 及其 ARM/x64 适配。
- [x] **Bilibili Integration**: `yt-dlp` 针对 B站英文视频字幕的参数调优。
- [x] **Local Media Support**: 本地音视频导入与 `ffmpeg` 预处理接口。
- [x] **DeepSeek/OpenAI Compat**: `ai_client_factory.py` 的通用化重构。
- [x] **Launcher Implementation**: `Run_Ear2Finger.py` (无窗口启动 + 浏览器唤醒)。
- [x] **TCP Connection Watchdog**: 跨平台的自杀锁逻辑 (Mac 验证，Win 打包)。

## 4. 风险控制 (Risk Flags)
- **静态资源冲突:** 确保 API 路由优先级高于静态文件挂载。
- **权限问题:** Windows 下调用 `netstat` 可能需要特定权限。
- **端口冲突:** 默认使用 `9528` 端口，需增加随机端口重试机制。
